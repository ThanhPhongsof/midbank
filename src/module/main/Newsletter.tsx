import { LayoutContainer } from "components/layout";

const Newsletter = () => {
  return (
    <section className="newsletter">
      <LayoutContainer>Newsletter</LayoutContainer>
    </section>
  );
};

export default Newsletter;
