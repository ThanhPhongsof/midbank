import { LayoutContainer } from "components/layout";

const CTA = () => {
  return (
    <section id="faq" className="cta">
      <LayoutContainer>cta</LayoutContainer>
    </section>
  );
};

export default CTA;
