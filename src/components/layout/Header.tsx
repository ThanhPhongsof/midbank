import React from "react";
import LayoutContainer from "./LayoutContainer";

const Header = () => {
  return (
    <header className="header">
      <LayoutContainer>header</LayoutContainer>
    </header>
  );
};

export default Header;
